import { CaptionOutput, QueryOutput, DetectOutput, PointOutput, SegmentOutput, SegmentStreamOutput, CaptionRequest, QueryRequest, DetectRequest, PointRequest, SegmentRequest } from './types';
export interface MoondreamVLConfig {
    apiKey?: string;
    endpoint?: string;
}
export declare class vl {
    private apiKey;
    private endpoint;
    constructor(config: MoondreamVLConfig);
    private encodeImage;
    private makeRequest;
    private streamResponse;
    caption(request: CaptionRequest): Promise<CaptionOutput>;
    query(request: QueryRequest): Promise<QueryOutput>;
    detect(request: DetectRequest): Promise<DetectOutput>;
    point(request: PointRequest): Promise<PointOutput>;
    private streamSegmentResponse;
    segment(request: SegmentRequest): Promise<SegmentOutput | SegmentStreamOutput>;
}
